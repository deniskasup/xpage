mixin svg(name, vbox, w, h)
    - mod = mod || ''
        svg(xmlns='http://www.w3.org/2000/svg' version='1.1'  class=`icon icon-`+ name, viewBox = vbox, width = w, height = h)
            use(xlink:href="#" + name)
            